// ignore_for_file: prefer_const_constructors
import 'package:cinema_models/cinema_models.dart';
import 'package:test/test.dart';

void main() {
  group('CinemaModels', () {
    test('can be instantiated', () {
      expect(CinemaModels(), isNotNull);
    });
  });
}
